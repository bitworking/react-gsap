---
name: Controls
menu: Components
---

import { Fragment } from 'react';
import { Playground, Props } from 'docz'
import { Controls, PlayState } from './../../../react-gsap/src/'
import { Tween } from './Tween'

# Controls

The Controls component is a helper that you can use to debug your animations.
Just wrap it around a Tween or Timeline component and you'll get control buttons and a slider.

With the optional `playState` prop you can set an initial play state.

You can use the `PlayState` enum/object or just the strings "play", "restart", "reverse", "restartReverse", "stop", "stopEnd", "pause" or "resume".

    import { Controls, PlayState, Tween } from 'react-gsap';

### Usage with Tween

<Playground>
  <Controls playState={PlayState.restartReverse}>
    <Tween to={{ x: '200px', rotation: 180 }} duration={2} ease="back.out(1.7)">
      <div style={{ width: '100px', height: '100px', background: '#ccc' }} />
    </Tween>
  </Controls>
</Playground>

